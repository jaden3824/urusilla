# Urusilla Claim-to-Evidence Matrix

Status: living publication gate  
Evidence cut-off: 2026-08-20  
Rule: absence of contrary evidence is never treated as support

Headline boundary: demonstrated saving for general communication between unfamiliar agents is currently **0%**. In the frozen 2,542-turn broad lane, H1 exact no-regret passes, H2 general compact value and H3 repeated-context value fail, and H4 end-to-end utility is not evaluated. Warm carrier saving is 0.65% to 0.80%; cold and post-decode API-input savings are 0%. The retained 42-record lane has 0/168 bound and 0/168 standalone compact wins, with 2.24% to 3.00% standalone cold overhead. Total tokens per safely completed real-model task are unknown. Narrower in-domain and synthetic percentages must not be substituted for that missing result.

## Status vocabulary

- **Supported:** the current artifact directly measures the bounded claim.
- **Partially supported:** evidence exists, but scope, sample size, independence, or comparability is insufficient for the intended paper claim.
- **Negative:** the current evidence contradicts the desired claim under at least one measured condition.
- **Pending:** no claim-eligible experiment exists.
- **Blocked:** a prerequisite must be resolved before the experiment or claim is valid.

## Core evidence matrix

| ID | Proposed claim | Current evidence | Status | Missing evidence or limitation | Release gate |
|---|---|---|---|---|---|
| C01 | The canonical semantic kernel round-trips deterministically. | Current repository status is delegated to commit-bound CI; fixed corpora round-trip under every measured codec. A separately written same-project Node implementation agrees byte-for-byte with 280 Python-oracle-derived v0.2 fixtures and rejects 25 frozen negative fixtures. Exact research dependencies are recorded in `requirements-research.lock`. | Supported for the pinned project implementations and frozen corpora. | The Node lane is project-internal and oracle-derived, not external independent reproduction. A release still needs immutable platform/container identity, package hashes, and externally implemented vectors. | Two externally independent implementations must pass the same published golden and negative vectors under commit-bound environment manifests. |
| C02 | The validator fails closed on malformed or semantically invalid messages. | Tests cover unknown shadow fields, invalid act-body pairs, malformed commitments, replay, causal state, unknown executable extensions, resource limits, corruption, canonical re-encoding, and 17 added public-decoder boundary cases. Complete-module combined A2A/v0.2 branch coverage is 82.89%. | Supported for tested cases. | Coverage-guided fuzzing, independent security review, and production transport threats. | Publish fuzz corpus, sanitizer results where applicable, and an external audit response. |
| C03 | The warm binary profile is smaller than strong standard byte codecs. | Under independent message resets, warm v0.2 is 54,752 bytes versus 168,809 for per-message gzip JSON, 219,055 for MessagePack, 219,899 for CBOR, and 229,790 for typed Protobuf. The 1,402-byte profile is included in cold accounting. A 378-combination reset sweep yields a per-representation byte frontier on which cold v0.2 first beats the byte-best integrity-matched checked-JSON frontier at the tested 64-message point and is 9.40% smaller at 280, with exact deterministic recovery throughout. | Partially supported under a per-record integrity contract; negative as a blanket byte claim. | The cold profile never beats the byte-best bare-JSON frontier on the 21-point grid; fixed compressors have different crossover sets. One synthetic in-domain corpus, no FlatBuffers, Cap'n Proto, alternative Protobuf schemas, native implementations, external compressed traffic, or packet-loss analysis. | Reproduce on at least two preregistered external corpora and publish the Pareto frontier by framing, reset, integrity, bytes, and latency. |
| C04 | The warm binary profile is universally smaller. | In the reset-sweep frontier, independently cold v0.2 never beats byte-best bare JSON and is 10.20% larger at 280 messages. The cached frontier beats byte-best bare JSON through the tested 128-message point, then loses at 140, 256, and 280; at 280 it is 4.33% larger. | Negative as a universal claim. | Fixed compressors can have different crossover sets, and the ranking reverses when JSON carries equivalent independent per-record checksums, demonstrating contract and reset dependence rather than a single winner. | Permanently prohibit universal wording; report distribution- and contract-specific Pareto results. |
| C05 | The tokenizer-aware surface reduces receiver tokens on unseen combinations. | On the 224/56 grouped split, v0.3 holdout savings are 37.8% to 41.8% versus Base64 and 62.0% to 63.4% versus minified JSON for its two-tokenizer study. Grouped-holdout v0.4 uses 59.64% to 69.79% fewer tokens than controlled terse English across four pinned tokenizers; exact recovery holds. | Partially supported. | The holdout comes from the same generator family and the codebook/tokenizer alphabet were project-designed. | Preregister external schemas, independent messages, unseen tokenizers, and held-out model families. |
| C06 | The specialized surface generalizes to arbitrary novel traffic. | The v0.4 project-authored OOD surface uses 49.15% to 103.15% more tokens than controlled terse English. On 43 premeasurement-sealed official external examples, v0.4 uses raw fallback for every record, with an 83.9% raw-symbol rate and 70.6% raw-byte coverage. The retained 42-record corpus remeasures fallback rather than an amortized compact selection. | Negative. | Both external-example samples use source-preserving project wrappers, not representative deployed traffic or independent operator reproduction. | Keep complete raw fallback, prohibit universal wording, and test specialized candidates on independently operated representative traffic without tuning on the evaluation set. |
| C07 | Exhaustive warm selection never increases receiver tokens relative to the enumerated codec-valid candidates. | For each frozen message and negotiated tokenizer, the implementation constructs every codec-valid candidate and selects the exact minimum. Checks cover 1,160 project-corpus pairs, 172 first-external-corpus pairs, and 168 second-external-corpus bound choices. | Supported as an implementation invariant for the frozen candidate sets and tokenizers. | Authorization, provenance, privacy, authentication, and replay-policy eligibility were not exercised end to end; nor were representative traffic, tokenizer drift, model-based translation cost, or adversarial candidates. This is not a learned selector result. | The pre-receiver-call chooser must retain non-positive regret on preregistered public traffic without future-session information while a separate deployment policy enforces safety eligibility. |
| C08 | Adaptive selection improves out-of-domain efficiency. | The ten-message OOD set shows a post-hoc v0.6 warm reduction of 1.87% to 5.42% relative to v0.5. The first 43 official-example wrappers fail the 20% cold value gate. Transparent v0.8 records 0/172 compact wins under both bound and standalone contracts there; standalone text is 5.85% to 6.80% larger. On the retained 42-record corpus, bound and standalone compact modes each win 0/168; every standalone cold plan selects terse and remains 2.24% to 3.00% above raw plain text. | Negative for compact out-of-domain compression; supported for exact transparent fallback under the measured contracts. | Ties are not savings. Both samples use conservative project wrappers, and the retained run is exploratory remeasurement rather than fresh confirmation, representative traffic, or independent reproduction. | Evaluate bound and standalone contracts on independently operated traffic and in end-to-end model tasks; retain raw plain fallback and latency accounting. |
| C09 | For frozen known sequences, exhaustive accounting selects the lower-token cold plan. | The v0.6 planner chooses activation only when beneficial. The v0.7 planner evaluates 64 states and activates no receiver profile in any of 12 development, holdout, or OOD plans at the tested sequence lengths. | Supported only as offline token accounting. | A deployable policy does not know the future horizon and must count cache churn, discovery, signatures, negotiation, CPU, latency, repair, and money. | Evaluate a calibrated pre-session activation policy on unknown-horizon public traces. |
| C10 | A prompted receiver reliably reconstructs canonical JSON messages. | Two historical pre-cutover live trials reconstruct 13/14 and 14/14 messages; combined 27/28. Earlier protocol summaries range from 0/14 to 11/14. No provider call was rerun on current Urusilla inputs. | Negative under the predeclared local stopping gate. | One model, 14 synthetic messages, shape-revealing output schema, repeated context, protocol amendments after observed failures, and two final repeats. This is not current-input evidence or an understanding measure. | A future preregistered study must meet its exactness threshold across all declared model families before reconstruction-format ranking. |
| C11 | A prompted receiver reconstructs the compact symbolic surface as reliably as JSON or controlled English. | Offline codec round trips only; the live matrix stopped after the JSON gate failed. | Pending. | No live symbolic or controlled-English receiver cells. | Run identical public-task and reconstruction cells only after the baseline reconstruction gate passes. |
| C12 | Agents reliably construct valid typed messages from Capsule-guided intents. | A neutral-ID pilot selects 16/16 emit-or-reject decisions and 10/10 acts, but only 6/10 generated messages pass structural validation and preserve essential semantics; the standardized score remains unset. | Negative. | Repairs, fallback, unseen partners, and end-to-end task use remain unmeasured. | Measure typed-message construction, validation failure, repair, fallback, and final task success on blinded public tasks. |
| C13 | The system reduces total tokens per safely completed task. | A zero-call preflight freezes 5,382 initial prompts and finds the adaptive arm largest in every dataset-tokenizer row. The offline harness then executes 108 deterministic gold-using mock episodes and a complete ledger, but its adaptive cold accounting is unfavorable and the mock is explicitly ineligible as performance evidence. No provider task run exists. | Pending; current cold and mock evidence is unfavorable or non-empirical. | No claim-eligible end-to-end public-task ledger with real model outputs, repairs, billed reasoning, final answers, and safe task success. | On at least two public task families, the one-sided 95% lower bound must establish success non-inferiority and the lower bound of the two-sided 95% paired-bootstrap token-reduction interval must be at least 25%. |
| C14 | The system is near the strongest published communication efficiency. | No same-workload end-to-end comparison exists. Favorable local serialization percentages and published paper percentages use different tasks, models, denominators, and token boundaries. | Blocked by non-comparability. | No claim-eligible shared workload, model matrix, success gate, complete token ledger, or independent reproduction. | Same-workload reproduction within five percentage points on at least two task families without a safe-task-success regression. |
| C15 | The system is state of the art. | No same-workload end-to-end result or independent reproduction. | Pending and prohibited. | All principal competitive evidence is missing. | Outperform strongest reproducible baseline on two task families and obtain external reproduction. |
| C16 | For the fixed representative HTTP/1.1 SendMessage application-request shapes, warm v0.2 rows are smaller than structured DataPart rows. | Representative raw requests: 266,576 versus 396,258 bytes; gzip: 212,168 versus 260,187; exact 280/280. | Supported for the fixed request shapes and in-domain warm profile. | These are not complete transactions: authorization headers, TLS/TCP, responses, streaming, SDK behavior, network loss, and deployed endpoints are excluded. | Run two independent A2A endpoints and count complete bidirectional traces. |
| C17 | The private experimental A2A adapter enforces its pinned structural contract. | Tests validate extension activation, versioning, source IDs, identity binding, size caps, and sender constraints. | Supported for the tested private mapping. | This is not official conformance, deployed cross-play, or a security audit. | Publish the private mapping and negative vectors; retain experimental labeling. |
| C18 | Tested adapter and internal bridge paths preserve their compact source ID field. | Source ID pinning, adapter checks, and internal bridge pilots. | Supported only for those tested paths. | No signed production manifest is traced across every representation; several benchmarks use placeholders or do not exercise manifest resolution. | Create a commit-pinned signed manifest and verify resolution and preservation across every public arm. |
| C19 | The system saves energy. | Normalized sensitivity model shows outcomes can range from negative to positive depending on communication share, overhead, and success. | Pending; token savings are not energy evidence. | No wall-plug or device energy measurement. | Paired hardware measurement per safely completed task with confidence intervals. |
| C20 | The system is faster. | Several compact paths are slower in Python. On the retained 42-record v0.8 corpus, bound encode/select p50 is 4.04–16.26 ms per message versus 15.48 microseconds for direct minified JSON. In v0.9, interval-eight adaptive session encoding is about 4.6 times slower than matched full encoding. Persistent Brotli-11 v0.2 encodes faster than bare JSON but decodes about 2.39 times slower. | Negative for a blanket latency claim; mixed Pareto evidence. | Paths perform different validation, candidate-construction, tokenization, and integrity work; native implementation and model-generation latency may change the total. | Report end-to-end p50/p95 and use Pareto wording rather than a universal speed claim. |
| C21 | The protocol evolves without silent semantic drift. | Immutable profiles, migration checks, lifecycle gates, rollback, cross-play requirements, and founder approval constraints are unit-tested. | Supported as a governance mechanism, not as long-term empirical behavior. | No multi-maintainer history or external deployment. | Public proposal process, signed releases, drift tests, and independent cross-play. |
| C22 | The protocol has external adopters. | Internal bridge pilots are explicitly marked internal and `external_adopter=false`; the Node cross-runtime lane is also same-project evidence. | Negative at present. | No third-party usage telemetry, independently operated implementation, or verified external cross-play. | Only count opt-in verifiable external events; publish zero when zero. |
| C23 | The project can safely replace general web search. | Internet-layer architecture preserves retrieval, source access, rights, deletion, and provenance rather than eliminating search infrastructure. | Pending and too broad. | No web-scale ingestion, freshness, rights, deletion, or adversarial evaluation. | Frame as an agent-mediated retrieval layer and stage deployment behind source-preserving gates. |
| C24 | Latent transfer is a superior universal communication language. | Architecture review and literature show model compatibility, privacy, wire-size, and attack limitations. | Negative as a universal claim. | No compatible-model latent benchmark in this project. | Treat latent transfer as an optional sidecar with a canonical external semantic record. |
| C25 | Readable fallback text is safe without integrity binding. | Raw controlled terse English accepts 284/2,240 deterministic mutations as different canonical semantic messages; the checksummed envelope rejects 2,240/2,240. | Negative. | Bounded mutations do not model an attacker who recomputes checksums. | Bind exact message bytes, framing, session, and sequence through an authenticated record transport or signed/MACed envelope; use the checksum only for accidental corruption. |
| C26 | Human auditors reconstruct normative fields and detect material errors faster or more accurately than with JSON or controlled terse English. | Deterministic human-readable rendering exists, but no blinded human study measures reconstruction, error detection, time, or agreement. | Pending. | Syntactic readability is not demonstrated audit utility. | Run a preregistered blinded study with field accuracy, material-error detection, completion time, and inter-rater agreement. |
| C27 | The adapter is officially A2A-interoperable and secure. | No official conformance certification, independent deployed peer, penetration test, or security audit exists. | Pending and prohibited as a current claim. | Private structural tests cannot establish interoperability or security. | Pass independent A2A cross-play and a scoped security audit. |
| C28 | Agents directly emit a canonical compact surface without semantic loss. | No direct compact-surface generation experiment exists. | Pending. | The Capsule pilot constructs typed messages, not canonical v0.4/v0.6 or binary surface bytes. | Measure direct emission, parse validity, semantic fidelity, repair, and fallback across held-out models and tasks. |
| C29 | Receiver-bound v0.7 profiles improve the measured warm grouped holdout without OOD regression. | The guarded chooser saves 23,997 development tokens and 4,302 grouped-holdout tokens, retains v0.6 for every OOD choice and saves 0 there, and recovers and re-encodes 3,480/3,480 direct profile cases exactly. | Partially supported as project-corpus serialization evidence. | The grouped holdout shares the generator family, cold activation is 0/12 at tested lengths, raw symbols are decoder-before-model only, and selection is slower. | Demonstrate oracle-free activation and exact decoded-before-model use on sequestered external sessions long enough to amortize the profile. |
| C30 | Transparent v0.8 fallback prevents positive bound receiver-token regret on retained OOD examples. | On the retained 42-record corpus, both bound and standalone compact modes record 0/168 strict wins. Bound totals tie raw plain text; standalone cold selects terse for all records and remains 2.24% to 3.00% larger. Exactness, deterministic re-encoding, and retained mutation checks pass. | Supported as a bounded fallback invariant; negative as a compression claim. | This is exploratory current-artifact remeasurement of a retained corpus, not fresh external preregistration. The authenticated transport binding is modeled, not deployed; no representative traffic, model, task, or independent operator is evaluated. | Reproduce with an independently implemented authenticated binding and representative traffic; report tokens, bytes, latency, task outcome, and compact-win rate separately. |
| C31 | Checkpointed v0.9 deltas reduce repeated-state serialization cost. | At interval eight on 24 synthetic correlated sessions, complete matched-frame token savings are 53.71% to 55.15%; recovery/reselection pass for 18,432/18,432 tokenizer-specific snapshots and 4,608/4,608 representative mutations are rejected. | Partially supported for the frozen synthetic state-sync workload. | Correlation is deliberate, no model consumes deltas, state retention is assumed, retransmission and compressed-stream baselines are outside the comparison, and adaptive encoding is slower. | Evaluate independently authored state traces, matched compression, loss/retransmission cost, model-facing expansion policy, and end-to-end task success. |
| C32 | The Node lane independently reproduces the principal result. | It is separately written and cross-runtime, but uses same-project specification inputs and Python-oracle-derived vectors. | Negative as external independent reproduction; supported only as internal compatibility evidence. | No external operator, clean-room oracle-independent vectors, immutable release commit, signed report, or unseen cross-play. | External implementers must work from the public specification and published vectors, disclose provenance, and reproduce the principal claim on an independently operated environment. |
| C33 | One universal lossless compact carrier improves broad ordinary dialogue. | The frozen four-family, 2,542-turn lane passes H1 exact no-regret but fails H2 general compact value and H3 repeated-context value; H4 is not evaluated. Warm saving is 0.65% to 0.80%, compact choices cover only 7 to 9 turns per tokenizer, and cold and post-decode API-input savings are 0%. | Negative for current general compact value; supported for the fallback invariant. | Convenience sample, project-internal freeze chronology, authors could access the corpus, no model task, and no total-token ledger. | Stop incremental universal-lossless tuning; reopen only for a separately frozen architecture-changing hypothesis. |
| C34 | Gold public action/state proves deployable task-level savings. | On 399 SGD next-action prompts, the gold-state oracle reduces prompt tokens by 7.48% to 23.34% across four tokenizers. It calls no model and measures no accuracy. | Pending as a deployable method; supported only as an oracle opportunity bound. | Dataset gold state replaces raw history, prose is intentionally lost, and parser, action accuracy, output, repair, safety, and task success are unmeasured. | Build an oracle-free public-state producer and pass end-to-end task-success, semantic-fidelity, safety, repair, and total-token gates. |

## Paper table plan

| Table | Content | Current readiness |
|---|---|---|
| T1 | Semantic acts, typed fields, and observable protocol effects | Ready after specification and release-revision freeze. |
| T2 | Payload bytes versus JSON, gzip JSON, CBOR, MessagePack, and typed Protobuf | Ready as bounded in-domain evidence; add external corpora. |
| T3 | Grouped holdout and OOD tokens across four tokenizers | Ready with negative results retained. |
| T4 | Adaptive warm/cold choices, regret, bytes, and latency | Ready for frozen corpus; add oracle-free public-task trace. |
| T5 | Live receiver exactness, validation, repairs, tokens, latency, and cost | Ready as a failed pilot gate, not a format comparison. |
| T6 | Public-task success and total-token non-inferiority | Missing; primary submission blocker. |
| T7 | Cross-model and unseen-pairing transfer | Missing. |
| T8 | Safety, provenance, injection, replay, and collusion evaluation | Partially ready; adversarial expansion missing. |
| T9 | Energy per safely completed task | Missing. |
| T10 | Broad lossless carrier and public action-state results | Ready as H1 pass, H2-H3 fail, H4 not evaluated, plus a no-accuracy SGD oracle bound. |

## Figure plan

1. Layered architecture: semantic kernel, protocol state, profiles, adaptive selector, transport, and readable fallback.
2. Pareto plot: task success versus total tokens, wire bytes, latency, and cold cost.
3. Generalization plot: development, grouped holdout, and OOD representation choices by tokenizer.
4. Claim ladder: codec evidence, comprehension, public-task utility, cross-play, and independent reproduction.

## Immediate publication work queue

1. Keep the completed A0 public-task preflight snapshot frozen and fail closed on digest drift.
2. Keep the completed six-arm offline harness and unfavorable mock/cold results frozen; install the exact upstream prompt sources before any claim-ready run.
3. Keep the universal lossless text lane stopped unless a separately frozen architecture-changing hypothesis is proposed.
4. Implement oracle-free public action-state, model-native/task-aware, verified-silence/topology, negotiated-routine, and raw-fallback arms; then pass a small real-model reliability gate before expanding paid calls.
5. Run two task families and the three-family model matrix.
6. Publish paired raw observations and preregistered confidence analysis.
7. Publish the same-project Node compatibility lane, then obtain an external independent implementation and cross-play suite.
8. Measure energy only after task success and latency stabilize.
9. Regenerate every compatibility-sensitive digest against the immutable public-release revision.
